= render 'subnav', model: 'settings'
- if @settings && @settings.length > 0
  %table.table.table-condensed.table-striped
    %thead
      %tr
        %th
        %th Параметр
        %th Значение
        %th
    %tbody=render @settings
- else
  = render 'backend/shared/no_results_message'